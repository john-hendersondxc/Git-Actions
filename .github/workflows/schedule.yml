name: Schedule Comands

on:
  schedule:
    - cron:  '0/5 * * * *'

jobs: 
  run-windows-command:
    runs-on: windows-latest
    steps:
      - name: Directory Bash
        run: echo "Current run time is- $(date +%Y-%m-%d) at $(date +%H.%M.%S)"
        shell: bash

